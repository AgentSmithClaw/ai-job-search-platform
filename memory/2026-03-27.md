## 2026-03-27

- 将当前前端恢复并整合为完整产品工作区：分析、历史、投递、任务、面试、账单、设置、首页、认证全部接回真实后端。
- 当前前端基于 React + TypeScript + Vite，不再是旧文档里描述的 Vanilla HTML/CSS/JS。
- 修复了 `app/main.py` 中限流逻辑误把全局请求数套到路径限流上的问题，避免 `/api/analyze` 被误拦截。
- 扩展 `tests/smoke_test.py`，覆盖 auth me/profile、orders、dashboard、analysis、history/export、applications/tasks/interview prep、refund。
- 完成 Bearer Token 改造：
  - 前端请求统一使用 `Authorization: Bearer <token>`
  - 后端保留 query/body `access_token` 兼容旧调用
  - smoke test 已切到 Bearer 模式并通过
- 完成数据库生产化改造：
  - `app/db.py` 支持 SQLite 默认 + `DATABASE_URL` 切换 PostgreSQL
  - `requirements.txt` 新增 `psycopg[binary]`
  - `docker-compose.yml` 内置 PostgreSQL 服务
  - `.env.example` 增加 `DATABASE_URL` 与 `CORS_ORIGINS`
- 完成产品体验打磨：
  - 结果页重构为更可信的报告结构，补上 competency breakdown、复制摘要、导出 loading、跳转应用/任务/面试入口
  - 设置页重构为更正常的账户/外观/账单/历史入口页
  - 历史页、账单页修复乱码和异常分隔符，统一成更正常的 SaaS 风格
  - AppShell / Topbar / PageHeader / 全局 CSS 做了移动端与壳层收口
- 文档已重写：
  - `README.md` 更新为当前真实架构、Bearer Token、Postgres、Docker Compose
  - `docs/api.md` 更新为 Bearer Token 优先的接口说明
- 当前验证结果：
  - `python tests\\smoke_test.py` -> 31/31 passed
  - `python -m pytest tests -q` -> 51 passed
  - `npm run build` -> passed
- 额外说明：
  - 尝试本地用 Docker Compose 拉起 Postgres 做实库验证，但当前机器未安装 Docker，因此 PostgreSQL 支持目前属于代码与配置已完成、SQLite 路径已验证、Postgres 实库尚未在本机跑通。
